namespace CS_DHT;

public class Tests
{
	private readonly Question[] _questions = [
		new Question("Какова главная цель консистентного хеширования в DHT?", 1, [
			"Минимизировать количество ключей, перемещаемых при добавлении или удалении узла",
			"Обеспечить, чтобы каждый узел хранил строго одинаковое число ключей",
			"Гарантировать логарифмическую длину маршрута поиска",
			"Равномерно распределить IP-адреса узлов по пространству имён"
		]),
		new Question("Каким образом Chord отображает ключи на узлы?", 4, [
			"Через двумерное координатное пространство, в котором каждый узел отвечает за ближайший квадрат",
			"Через префиксное дерево, где ключ соответствует ближайшему префиксу узла",
			"Через случайный выбор k ближайших соседей по сетевой задержке",
			"Ключ назначается первому узлу, идентификатор которого равен или следует за " +
			"хешем ключа по часовой стрелке на кольце"
		]),
		new Question("Какова асимптотическая сложность поиска ключа в Chord при стабильном состоянии?",
			3, [ "O(1)","O(N)","O(log N)","O(N^{1/d})"
		]),
		new Question("Что происходит первым делом, когда новый узел присоединяется к кольцу Chord?",2, [
			"Он копирует finger table у своего successor и сразу начинает обслуживать запросы",
			"Он вычисляет свой идентификатор, находит через существующий узел своего successor" +
			" и устанавливает собственный successor-указатель",
			"Он запрашивает полный список узлов у любого участника и строит маршрутную таблицу",
			"Он рассылает broadcast-сообщение, чтобы все узлы обновили свои finger tables"
		]),
		new Question("Какой основной принцип маршрутизации используется в Pastry?", 3, [
			"Жадное продвижение к ближайшему узлу в евклидовом пространстве",
			"Маршрутизация по хешу с учётом географических координат",
			"Префиксная маршрутизация, при которой на каждом шаге увеличивается " +
			"совпадение префикса адреса узла с ключом",
			"Случайные блуждания с экспоненциальным усечением"
		]),
		new Question("За счёт чего Pastry учитывает сетевую близость (proximity)?", 4, [
			"Заменяя leaf set на список физически ближайших узлов",
			"Рассчитывая координаты узлов на основе времени круговой задержки",
			"Игнорируя сетевую близость и полагаясь только на логический префикс",
			"При заполнении routing table выбирается узел с наименьшей задержкой " +
			"среди кандидатов с подходящим префиксом"
		]),
		new Question("Как новый узел инициализирует свою таблицу маршрутизации в Pastry?", 2, [
			"Скачивает полную копию таблицы от своего leaf set",
			"Получает начальные данные от ближайшего по префиксу узла и запрашивает " +
			"недостающие строки у узлов, пересылающих сообщение присоединения",
			"Вычисляет её самостоятельно, перебирая все возможные префиксы",
			"Строит её на основе списка физических соседей, полученного от bootstrap-сервера"
		]),
		new Question("Что представляет собой координатное пространство в CAN?", 4, [
			"Кольцо идентификаторов в диапазоне [0, 2^m)",
			"Дерево префиксов с переменной глубиной",
			"Многомерное гиперкубическое пространство с фиксированной длиной ребра",
			"d-мерный тор, разделённый между узлами на непересекающиеся зоны"
		]),
		new Question("Как происходит разделение зоны при добавлении нового узла в CAN?", 2, [
			"Узел выбирает случайную точку и забирает всю зону, если попадает в неё",
			"Существующий узел делит свою зону пополам по одному из измерений, " +
			"передавая половину новому узлу",
			"Все узлы равномерно уменьшают свои зоны, освобождая место для нового",
			"Новая зона создаётся в центре тора путём вытеснения нескольких соседей"
		]),
		new Question("Какова сложность поиска в CAN при равномерном разделении d-мерного" +
			" пространства на N зон?", 3, [ "O(log N)","O(d * N)","O(d * N^{1/d})","O(N / d)"
		]),
		new Question("В каком случае CAN может оказаться предпочтительнее Chord?", 2, [
			"Когда критично малое количество переходов на любом масштабе сети",
			"Когда требуется гибкость в выборе соседей, позволяющая учитывать физическую " +
			"близость, и размер таблицы маршрутизации не зависит от N",
			"Когда ключи всегда располагаются на соседних узлах в порядке сортировки",
			"Когда все узлы находятся в одной локальной сети"
		]),
		new Question("Что понимается под «реальностью-близостью» в CAN и как её можно улучшить?", 2, [
			"Гарантированное расположение всех соседей зоны в пределах одного дата-центра",
			"Соответствие логических соседей по зоне физически близким узлам; " +
			"достигается эвристиками выбора соседа с минимальной задержкой при делении зон",
			"Способность CAN функционировать без виртуальных координат, опираясь " +
			"на реальные географические адреса",
			"Свойство, при котором каждый узел знает точное физическое расстояние до всех остальных"
		]),
		new Question("На чём основана маршрутизация в Tapestry?", 3, [
			"На кольцевой структуре и finger-таблицах, указывающих на экспоненциально удалённые узлы",
			"На координатной маршрутизации в многомерном пространстве",
			"На суффиксном сопоставлении: каждый шаг увеличивает совпадение суффикса " +
			"идентификатора следующего узла с целевым ключом",
			"На случайных переходах с вероятностью, пропорциональной близости суффикса"
		]),
		new Question("Как Tapestry обеспечивает отказоустойчивость и балансировку через репликацию?", 4, [
			"Хранит идентичные копии объекта на всех узлах сети",
			"Использует центральный координатор для назначения реплик",
			"Реплицирует объекты только на физически соседние узлы",
			"Резервирует объекты на нескольких следующих по близости суффикса узлах, " +
			"а в таблицах маршрутизации держит несколько резервных указателей на каждом уровне"
		]),
		new Question("Размер таблиц состояния узла в пересчёте на количество записей:", 3, [
			"Chord – O(N), Pastry – O(N), CAN – O(1), Tapestry – O(log N)",
			"Chord – O(log N), Pastry – O(log N), CAN – O(log N), Tapestry – O(log N)",
			"Chord – O(log N), Pastry – O(log N), CAN – O(d), Tapestry – O(log N)",
			"Chord – O(d * N^{1/d}), Pastry – O(log N), CAN – O(d), Tapestry – O(N)"
		]),
		new Question("В чём принципиальное различие между однонаправленным кольцом " +
		"Chord и пространством CAN?", 3, [
			"Chord быстрее, а CAN медленнее при любых условиях",
			"Chord использует консистентное хеширование, а CAN – нет",
			"Chord жёстко фиксирует соседей по кольцу, тогда как CAN даёт" +
			" свободу выбора соседей по зонам, что упрощает учёт физической топологии",
			"Chord может обрабатывать только точные запросы, а CAN – диапазонные"
		]),
		new Question("Какое допущение о сетевой среде является общим для Chord, Pastry и " +
		"Tapestry, но не обязательным для CAN?", 1,[
			"Полная связность (любой узел может напрямую связаться с любым другим), " +
			"в то время как CAN может работать, опираясь только на локальные ссылки " +
			"между соседями по зонам",
			"Статический состав узлов без отказов",
			"Все узлы должны находиться в одном широковещательном домене",
			"Наличие выделенного сервера для координации"
		]),
		new Question("В каком сценарии Tapestry предпочтительнее Chord?", 3, [
			"Требуется минимальная задержка и ровно один переход для любого поиска",
			"Необходима простая кольцевая структура без сложной маршрутизации",
			"Требуется децентрализованная публикация объектов в ближайших к ним узлах " +
			"и устойчивость к отказам через суррогатную маршрутизацию без глобальной перестройки",
			"Сеть состоит менее чем из 10 узлов"
		]),
		new Question("Какой протокол в наименьшей степени зависит от глобальной информации " +
			"при построении оверлея?", 4, [
				"Chord, потому что finger table строится только по successor",
				"Pastry, потому что routing table заполняется на основе префиксов",
				"Tapestry, потому что для публикации достаточно одного корневого узла",
				"CAN, потому что узлу достаточно знать только своих непосредственных " +
				"соседей по зоне, чтобы маршрутизировать запросы"
		]),
		new Question("Почему выполнение диапазонных запросов нетривиально в базовой Chord?", 2, [
			"Потому что Chord не поддерживает поиск по значению, а только по ключу",
			"Потому что консистентное хеширование разрушает порядок ключей, распределяя " +
			"их равномерно по кольцу, и соседние ключи могут оказаться на разных узлах",
			"Потому что для этого требуется O(N) переходов всегда",
			"Потому что диапазонные запросы запрещены спецификацией Chord"
		]),
		new Question("Сравнение отказоустойчивости: почему при одинаковой доле отказавших узлов " +
		"Pastry может обеспечить более высокую вероятность успешного поиска, чем Chord?", 2, [
			"Chord вообще не имеет механизма обхода отказавших узлов",
			"Pastry использует одновременно leaf set и routing table с " +
			"несколькими кандидатами на каждом шаге, Chord полагается на последовательный " +
			"successor-список и finger table без резервных вариантов на каждом уровне",
			"В Chord каждый узел хранит полную копию всей DHT",
			"Pastry автоматически восстанавливает все ключи после отказа, а Chord – нет"
		])
	];

	public IEnumerable<Question> Questions { get; init; }

	private Tests(int count)
	{
		var random = new Random();
		this.Questions = this._questions.OrderBy(_ => random.Next()).Take(count);
	}

	public static Tests GetTests(int count) => new(count);

	public void RunTests()
	{
		int rightAnswers = 0;

		foreach (var question in this.Questions) {
			if (Tests.AskQuestion(question)) {
				rightAnswers++;
			}
		}

		Console.WriteLine("Число правильных ответов: {0}/{1}", rightAnswers, this.Questions.Count());
		Console.WriteLine("Нажмите любую кнопку, чтобы продолжить");
		Console.ReadKey(true);
	}

	private static bool AskQuestion(Question question)
	{
		for (; ; ) {
			Console.Clear();
			Console.WriteLine(question.Text);
			Console.WriteLine("Варианты ответа:");

			var answersCount = question.Answers.Count();
			for (int i = 0; i < answersCount; i++) {
				var answerText = question.Answers.ElementAt(i);
				Console.WriteLine(" {0} -- {1}\n", i + 1, answerText);
			}
			Console.WriteLine("Укажите номер ответа");
			var ch = Console.ReadKey(true).KeyChar;

			const char ESC = (char)27;
			if (ch == ESC) {
				return false;
			}
			if (ch < '1' || ch > '0' + question.Answers.Count()) {
				continue;
			}
			return question.IsRight(ch - '0');
		}
	}
}
